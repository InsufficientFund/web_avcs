from __future__ import unicode_literals

from django.db import models

class avcs_model(models.Model):
    """docstring for avcs_model"""
    data1 = models.CharField(max_length=30)

# Create your models here.
